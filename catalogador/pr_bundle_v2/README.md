PR bundle: AUTO-FIX changes (conservative pass A + handler safety)\n\nIncluded files (copies of modified/created files from workspace):\n\n- engine_ia/utils/http.py  -- new centralized HTTP helper (default timeout + RequestError)\n- engine_ia/analyze_client.py\n- engine_ia/train_small.py\n- engine_ia/github_codex_connector.py\n\n- app-desktop/Catalogador.App/Helpers/SimpleLogger.cs  -- new simple logger used across C# code\n- app-desktop/Catalogador.App/Services/ApiClient.cs  -- replaced Debug.WriteLine with SimpleLogger and safer error handling\n- app-desktop/Catalogador.App/Services/ExcelService.cs  -- replaced Debug.WriteLine uses with SimpleLogger.Warn where applicable\n- app-desktop/Catalogador.App/Views/MainWindow.xaml.cs  -- converted several UI event handlers from async void to void wrapper + Task pattern
\nHow to apply these changes locally (PowerShell):\n\n1) Review the files in this folder. If you want to apply them to your working copy, copy the files over the existing locations. From the repository root run (example):\n\n   # backup current files\n   mkdir .git-patch-backup; \n   Copy-Item -Path .\\pr_bundle_v2\\** -Destination . -Recurse -Force -Verbose -Exclude README.md\n\n2) Create a branch and commit the changes:\n\n   git checkout -B auto-fix/handlers-logger-http\n   git add -A\n   git commit -m "AUTO-FIX: convert async void handlers to Task-safe pattern; add SimpleLogger; centralize HTTP in python"\n\n3) Push branch to remote (optional):\n\n   git push -u origin auto-fix/handlers-logger-http\n\nNotes:\n- I could not run git here to make the commit because `git` is not available in this environment.\n- I also built the WPF project here and it compiled successfully (0 errors, a few nullable warnings).\n- If you want a single ZIP of these files I can create one as well (if you prefer).\n\nNext steps I can do for you:\n- Run the Python static auditor and include the updated report in the bundle.\n- Attempt to patch additional C# nullable warnings.\n- Produce a unified patch/diff suitable for `git apply` (would require original base versions for accurate diffs).\n\nTell me which of the next steps you want me to do, or run the PowerShell git commands above locally to create the PR branch from these files."}jsonwebtoken```